    <?php

    /*
    Implementa un ejemplo donde decidas si un número es par o impar usando un ternario
    */

    // Variable
    $hora = 22;

    // Operador ternario (Si es mayor de 12, el valor es 24 y si es menor, el valor es 12)
    $formato = ($hora > 12) ? 24 : 12;

    // Mostramos el formato
    echo "El formato es de " . $formato . " horas";


    ?>